aws-cdk-lib>=2.140.0,<3
constructs>=10.0.0,<11
cdk-nag>=2.27,<3  # app.py lints every synth with the AwsSolutions rule pack
boto3>=1.34  # generate_pipeline_config.py reads the deployed stack's outputs
